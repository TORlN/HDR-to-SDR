# Runtime dependency inputs. Keep these exact pins readable for review, then
# regenerate requirements-lock.txt with hashes whenever either input file
# changes. CI and release builds install only from that shared lock.
pillow==12.2.0
tkinterdnd2==0.5.0
